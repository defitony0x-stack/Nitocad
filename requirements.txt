cadquery==2.3.1
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
jinja2==3.1.2
anthropic==0.7.5
numpy>=1.26.4,<2.0
openai>=1.0.0
boto3>=1.34.0   # R2 (S3-compatible) storage - see storage.py, optional at runtime

# MCP / A2MCP (a2mcp/) - real OKX Payment SDK integration, ported from
# Stitchfren's backend/cutline/requirements.txt. okxweb3-app-x402 provides
# the x402.http.* imports a2mcp/x402.py uses; fastmcp provides the MCP
# protocol server a2mcp/server.py mounts at /mcp. Neither version was
# independently confirmed against PyPI at time of writing (no network
# access in this environment) - check before relying on them, same caveat
# mcp-gateway's @x402/* pins had.
okxweb3-app-x402
fastmcp>=2.0

# NOTE on the anthropic pin above: it's an old SDK version left over from
# llm_parser.py (Claude integration). llm_parser.py is currently unused,
# cad_generator.py now imports DeepSeek's parser (deepseek_parser.py) or
# the regex fallback (smart_parser.py), not llm_parser.py. Left in place
# rather than removed, since llm_parser.py itself is still in the repo
# and someone may want to wire Claude back in instead of or alongside
# DeepSeek.
